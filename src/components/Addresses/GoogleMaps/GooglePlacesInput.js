import React, { useState, useEffect } from "react";

const GooglePlaceInput = ({ location, setLocation }) => {
  return <div id="map" className="googleMapLocator m-2"></div>;
};

export default GooglePlaceInput;
